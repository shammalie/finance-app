import { SomeText } from './components/some-text';

const Home = () => {
  return (
    <div className="flex min-h-[60vh] flex-col gap-8 text-center">
      <SomeText />
    </div>
  );
};

export default Home;
